/**
 * NBX_LIFE pipeline config — Life Insurance New Business.
 * 7 stages, 7 workflows, 17 steps, 42 task templates, 1 gated stage.
 */
import type { FlowPipelineDef, FlowStageDef, FlowWorkflowDef, FlowStepDef, FlowTaskTemplateDef } from '../types';
export declare const NBX_LIFE_CONFIG: {
    pipeline: FlowPipelineDef;
    stages: FlowStageDef[];
    workflows: FlowWorkflowDef[];
    steps: FlowStepDef[];
    tasks: FlowTaskTemplateDef[];
};
