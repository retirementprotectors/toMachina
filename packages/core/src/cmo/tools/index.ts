/**
 * CMO Tools Barrel (MUS-C01)
 * Empty until tool registration tickets (C03-C07) populate it.
 */
import type { CmoRegistryEntry } from '../types'

export const CMO_TOOLS: CmoRegistryEntry[] = []
