# 2HINOBI CCSDK — Discovery Brief for MUSASHI

> **Sprint 008 — "The Orchestrator Goes Online"**
> Compiled by 2HINOBI from MDJ1 code analysis, 2026-03-27

---

## Current State

2HINOBI is a Claude Code interactive session in a tmux session on MDJ1. NOT a service. NOT part of mdj-agent. A human-in-the-loop GA doing orchestration by hand.

**Limitations:**
1. No persistence across session restarts
2. Manual Slack polling (5-min cron, sometimes skips checks)
3. No HTTP interface — nobody can call 2HINOBI programmatically
4. Context loss risk on session death
5. No status endpoint
6. RONIN can't escalate to 2HINOBI — no programmatic path

---

## Target State

2HINOBI becomes Express route group `/shinobi` on mdj-agent (port 4200). Same service as VOLTRON and RONIN. Uses Agent SDK `query()` with Opus model.

### Endpoints

**POST /shinobi/message** — Receive Slack message, respond via 2HINOBI CCSDK
**POST /shinobi/check** — Cron fires every 5 min: read Slack, check RONIN, take actions
**POST /shinobi/escalate** — RONIN/RAIDEN call when blocked or need guidance
**GET /shinobi/status** — What is 2HINOBI doing right now
**POST /shinobi/sprint** — Queue a new sprint for RONIN

---

## Technical Requirements

- Agent SDK: `@anthropic-ai/claude-agent-sdk` — same `query()` as VOLTRON/RONIN
- Model: Opus (strategic reasoning, not execution)
- CCSDK Preflight: `bypassPermissions`, no maxTurns, no maxBudgetUsd, 3-attempt retry
- Slack: channel C0AP2QL9Z6X via `callMcpTool('mcp_slack_*')`
- JDM DM: user ID U09BBHTN8F2 for critical escalations
- Firestore: new collection `mdj_shinobi_state` for persistence
- Auth: existing `X-MDJ-Auth` middleware
- RONIN management: direct function calls to `forgeRunner` (same process)

---

## Integration Points

- **Slack:** Read C0AP2QL9Z6X, post responses, DM JDM at gates
- **RONIN:** Monitor `mdj_forge_runs`, auto-resume transient blocks, queue sprints
- **RAIDEN:** `/shinobi/escalate` ready for future RAIDEN integration
- **Firestore:** `mdj_shinobi_state/current` singleton doc for state persistence
- **gates.ts:** Add POST to `/shinobi/escalate` when RONIN hits a gate

---

## Reference Files for MUSASHI

| File | Why |
|------|-----|
| `/home/jdm/mdj-agent/src/server.ts` | Where to mount `/shinobi` routes |
| `/home/jdm/mdj-agent/src/forge/routes.ts` | RONIN route pattern to follow |
| `/home/jdm/mdj-agent/src/forge/runner.ts` | ForgeRunner — resume(), approve(), transient retry |
| `/home/jdm/mdj-agent/src/forge/claude-invoke.ts` | claudeInvoke() wrapper to replicate |
| `/home/jdm/mdj-agent/src/agent/voltron-sdk-client.ts` | deployGoal() CCSDK pattern |
| `/home/jdm/mdj-agent/src/tools/mcp-bridge.ts` | callMcpTool() for Slack |
| `/home/jdm/mdj-agent/src/forge/gates.ts` | Where to add escalation hook |
| `_RPI_STANDARDS/reference/os/CCSDK_AGENT_PREFLIGHT.md` | Mandatory defaults |

---

## Suggested Tickets (14 total, 4 phases)

### Phase 1 — Foundation
- TRK-S08-001: Route scaffold + server mount
- TRK-S08-002: Firestore state persistence
- TRK-S08-003: Slack channel reader/poster
- TRK-S08-004: RONIN status monitor

### Phase 2 — Core Logic
- TRK-S08-005: POST /shinobi/check implementation
- TRK-S08-006: 2HINOBI CCSDK agent core (shinobiQuery)
- TRK-S08-007: POST /shinobi/message implementation

### Phase 3 — Integration
- TRK-S08-008: POST /shinobi/escalate
- TRK-S08-009: POST /shinobi/sprint
- TRK-S08-010: GET /shinobi/status
- TRK-S08-011: gates.ts escalation hook
- TRK-S08-012: Cron configuration (setInterval in server.ts)

### Phase 4 — Polish
- TRK-S08-013: SHINOBI_OS_RULES constant
- TRK-S08-014: E2E smoke test

---

## Decisions for JDM

1. **Internal function calls vs HTTP** for RONIN management — recommend internal (same process)
2. **Gate auto-approval** — auto-retry transient CI, escalate JDM on plan/build gates
3. **Slack webhook vs polling** — webhook is instant but needs public endpoint via Tailscale

---

*Generated by 2HINOBI — The Architect, MDJ1*
