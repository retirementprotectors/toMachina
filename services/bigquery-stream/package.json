{
  "name": "tomachina-bigquery-stream",
  "version": "1.0.0",
  "description": "Cloud Function: Streams all Firestore document writes to BigQuery",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "deploy": "bash deploy.sh"
  },
  "dependencies": {
    "@google-cloud/bigquery": "^7.0.0",
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^5.0.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  },
  "engines": {
    "node": "20"
  }
}
