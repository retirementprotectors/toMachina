/**
 * Access route — STUB file created by Builder 3 for type-check compilation.
 * Builder 2 (sprint10/access-center-v2) will replace this with the full implementation.
 */
import { Router } from 'express'
export const accessRoutes = Router()
