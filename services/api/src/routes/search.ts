/**
 * Search route — STUB file created by Builder 3 for type-check compilation.
 * Builder 1 (sprint10/smart-search) will replace this with the full implementation.
 */
import { Router } from 'express'
export const searchRoutes = Router()
